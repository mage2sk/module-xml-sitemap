<?php /** @var \Magento\Backend\Block\Template $block */ ?>
<div style="background:#E3F2FD;border:1px solid #1976D2;border-radius:4px;padding:15px 20px;margin-bottom:20px;font-size:13px;line-height:1.7;">
    <strong style="font-size:14px;color:#1565C0;">XML Sitemap Profiles</strong>
    <p style="margin:8px 0 0;">
        Create and manage XML sitemap profiles with fine-grained control over which entity types, priorities,
        and change frequencies are included. Each profile generates its own sitemap files.
    </p>
    <ul style="margin:8px 0 0;padding-left:20px;">
        <li><strong>Entity Types:</strong> Choose which content types (products, categories, CMS pages, custom links) to include per profile.</li>
        <li><strong>Per-Type Settings:</strong> Configure change frequency and priority separately for products, categories, and CMS pages.</li>
        <li><strong>Image &amp; Video:</strong> Optionally include product images and video tags in your sitemap for rich search results.</li>
        <li><strong>Hreflang:</strong> Enable hreflang alternate tags for multi-language/multi-region stores.</li>
        <li><strong>Auto-Split:</strong> Sitemaps exceeding the max URLs per file (default 50,000) are automatically split with a sitemap index.</li>
        <li><strong>Cron Scheduling:</strong> Enable automatic regeneration on a custom cron schedule per profile.</li>
    </ul>
    <p style="margin:8px 0 0;color:#555;">
        <strong>CLI:</strong> Generate sitemaps from the command line with
        <code>bin/magento panth:seo:sitemap:generate</code> (all profiles) or
        <code>bin/magento panth:seo:sitemap:generate --profile-id=N</code> (specific profile).
    </p>
</div>
